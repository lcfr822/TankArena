using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace TankArena.Tanks
{
    public class BasicTank : BaseTank
    {
        // Start is called before the first frame update
        void Start()
        {
            InitializeTank();
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}